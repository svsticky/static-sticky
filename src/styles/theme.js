import { createMuiTheme } from 'material-ui/styles';

const theme = createMuiTheme({
    palette: {
      primary: {
        main: '#000078' 
      },
      secondary: { 
        main: '#11cb5f' 
      },
    },
  }
);

export default theme;